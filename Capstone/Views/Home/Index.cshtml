@model HomeViewModel

<partial name="_Carousel"/>

<h2>Projects of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div class="row">

    @foreach (var project in Model.ProjectOfTheWeek)
    {
        <partial name="_ProjectCard" model="project" />
    }

</div>
